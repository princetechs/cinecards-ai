# CineCards AI — provider API keys
# Copy to .env (gitignored) and fill values to enable real renders.
RUNWAY_API_KEY=
PIKA_API_KEY=
STABILITY_API_KEY=

# Optional analytics and SEO verification.
# Public by design: these values are rendered into the HTML when set.
PUBLIC_CLOUDFLARE_WEB_ANALYTICS_TOKEN=
PUBLIC_GOOGLE_SITE_VERIFICATION=
